# Sample ResourceGraphDefinition that materializes as a SimpleWebApp.
#
# Bridged from the infrastructure upstream example
# (examples/multicluster/simple-webapp-rgd.yaml) with railgrid labels +
# annotations added so the railgrid infrastructure provider's catalog
# picks it up:
#
#   railgrid.ai/expose=true            → mandatory; gates visibility
#   railgrid.ai/template-name          → catalog slug (the URL segment)
#   railgrid.ai/template-version       → required when provisioning
#   railgrid.ai/category, /cloud       → optional facet chips in UI
#   railgrid.ai/display-name           → human-readable name
#   railgrid.ai/description            → short blurb
#
# The provider's kro/templates.go reads exactly these keys (see
# the LabelExpose / AnnotationDisplayName etc constants in
# providers/infrastructure/kro/types.go).
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: simple-webapp
  labels:
    railgrid.ai/expose: "true"
    railgrid.ai/template-name: simple-webapp
    railgrid.ai/template-version: 0.1.0
    railgrid.ai/category: Workloads
    railgrid.ai/cloud: k8s
  annotations:
    railgrid.ai/display-name: "Simple webapp"
    railgrid.ai/description: "A web application — Deployment + Service, nginx by default. Scales via the replicas input."
spec:
  schema:
    apiVersion: v1alpha1
    kind: SimpleWebApp
    spec:
      name: string | required=true
      replicas: integer | default=1 minimum=1 maximum=10
      image: string | default="nginx:latest"
      port: integer | default=80
      serviceType: string | enum="ClusterIP,NodePort,LoadBalancer" default="ClusterIP"
    status:
      availableReplicas: ${deployment.status.availableReplicas}
      readyReplicas: ${deployment.status.readyReplicas}
      serviceEndpoint: ${service.spec.clusterIP}

  resources:
    - id: deployment
      template:
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: ${schema.spec.name}
        spec:
          replicas: ${schema.spec.replicas}
          selector:
            matchLabels:
              app: ${schema.spec.name}
          template:
            metadata:
              labels:
                app: ${schema.spec.name}
            spec:
              containers:
                - name: app
                  image: ${schema.spec.image}
                  ports:
                    - containerPort: ${schema.spec.port}
                  resources:
                    requests:
                      cpu: "50m"
                      memory: "64Mi"
                    limits:
                      cpu: "200m"
                      memory: "128Mi"

    - id: service
      template:
        apiVersion: v1
        kind: Service
        metadata:
          name: ${schema.spec.name}
        spec:
          type: ${schema.spec.serviceType}
          selector:
            app: ${schema.spec.name}
          ports:
            - port: ${schema.spec.port}
              targetPort: ${schema.spec.port}
